# Claude Code Instructions

## Sudo Commands

I cannot run sudo commands directly. When I need to run a sudo command, I should ask you to run it instead of trying to execute it myself.

For example, instead of running:
```bash
sudo tail -f /var/log/nginx/error.log
```

I should ask you to run it for me.
